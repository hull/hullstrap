deployment:
  neue:
    branch: neue
    commands:
      - bundle exec middleman s3_sync
dependencies:
  override:
    - bundle install --path vendor/bundle
test:
  override:
    - bundle exec middleman --verbose

